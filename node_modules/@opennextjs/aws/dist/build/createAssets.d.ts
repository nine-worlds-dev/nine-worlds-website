import * as buildHelper from "./helper.js";
export declare function createStaticAssets(options: buildHelper.BuildOptions): void;
/**
 * Create the cache assets.
 *
 * @param options Build options.
 * @returns Whether tag cache is used.
 */
export declare function createCacheAssets(options: buildHelper.BuildOptions): {
    useTagCache: boolean;
};
